# Verification scope

Run the local verification command from the repository root:

```powershell
npm.cmd run check
```

The command covers the Node-based simulator, source-boundary checks, and static asset/module references in this snapshot. Passing it does not prove installed-extension behavior, WebMCP support in a particular browser, accessibility acceptance, durable authorization, authentication, deployment, or external execution.

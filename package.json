{
  "name": "bounded-authorization-webmcp-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Human-authorized execution for WebMCP",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "ui:preview": "node scripts/serve-extension-preview.mjs",
    "test": "node --test",
    "check": "node --test && node scripts/check-static.mjs && node scripts/check-extension.mjs",
    "extension:check": "node --test tests/extension-ui.test.js && node scripts/check-extension.mjs",
    "t1:synthetic": "node scripts/run-t1-synthetic.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
